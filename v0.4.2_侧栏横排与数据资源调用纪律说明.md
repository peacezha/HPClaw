# HPClaw v0.4.2 侧栏横排与数据资源调用纪律说明

发布日期：2026-09-17

## 侧栏导航横排

- 四个主导航（对话/计算资源/文件/流程）改为侧栏顶部一排横排（图标在上、小字在下，徽标移到图标角标），不再上下堆砌占高度；"数据资源"移到底部工具行（与主题/QQ/更新并列）。

## 网络数据资源：先理解需求再调用

- Agent 提示词新增纪律：用户明确需要查公共数据库时才调用；需求模糊先反问澄清；本地/集群已有数据优先。
- **同轮去重**：同一端点同一组参数在同一轮内只真实调用一次，重复调用命中缓存并明确提示"请勿重复调用"（此前真实会话里出现过同一接口连调 3 次的无用功）。
- `search_web_apis` 输出现在带每个端点的**参数清单**（含必填标注），模型按清单传参，不再靠猜——之前 `unknown_param` 浪费调用轮的问题随之消失。
- 注册表补两个真实缺口：UniProt 新增"按登录号取 FASTA 序列"端点（/uniprotkb/{accession}.fasta，实测 200);NCBI EFetch 补 `seq_start`/`seq_stop`/`strand` 区段参数（可按染色体坐标区段取序列，实测取到目标区段 FASTA)。

## 验证

- TypeScript 静态检查通过（0 错误）。
- 全量自动化测试通过（新增参数清单输出、同轮缓存命中、缓存不复调上游等用例）。
- UniProt FASTA 与 NCBI 区段取序列两个新端点经真实网络验证可用。
