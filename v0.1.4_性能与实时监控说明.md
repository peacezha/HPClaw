# HPClaw v0.1.4 性能与实时监控改进

## 用户可见变化

- 流程步骤不再依赖 10 秒轮询才更新。创建运行、AI 更新步骤、登记作业号、作业完成后，服务端会立即把单条变化推送到当前集群标签页。
- 流程页与运行抽屉显示“实时同步中”；连接临时中断时显示“重连中”，恢复后自动补齐最新状态。
- 仍保留 60 秒低频快照作为断线补偿，避免网络抖动造成状态遗漏。
- 相同状态不会重复刷新整张流程页，运行卡片也不会因重复事件而重复出现。

## 卡顿治理

- 流程页和运行抽屉由每 10 秒各自扫描一次，改为 Socket 增量推送；远程 `run.json` 与调度器查询频率显著下降。
- BioSkills 全量生成校验由每次读取流程都扫描，改为每次程序启动仅执行一次，并发请求共享同一校验任务。
- 10 秒内的运行快照复用服务端缓存；流程创建或状态变化时立即失效，不牺牲实时性。
- 流程搜索、运行归属和卡片列表使用缓存计算，输入搜索词或展开卡片时不再反复做全量交叉扫描。

## 状态事件覆盖范围

- 正式运行记录创建。
- 步骤进入运行、完成、失败或跳过。
- Agent 自动登记 LSF/Slurm 作业号。
- 用户确认等待、环境阻塞、流程完成/失败等运行状态。
- 后台作业监控器和运行列表对账检测到的调度器完成状态。

## 验证

- TypeScript 类型检查通过。
- 80 个测试文件、550 项测试全部通过。
- 新增实时增量合并测试，覆盖原位更新、重复事件去重和低频快照补偿。
