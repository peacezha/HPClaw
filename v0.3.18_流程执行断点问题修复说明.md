# HPClaw v0.3.18 流程执行断点问题修复说明

发布日期：2026-09-16

## 解决的问题

对照一份真实流程运行记录，修复了导致"流程跑不完、中途各种报错"的四个叠加问题：

1. **环境探测烧光命令预算**：一轮 12 条命令里十几条在探 R 版本/conda 环境，正事没干预算就没了。现在**流程的命令预算只计会改变状态的命令**——ls/cat/module/which/conda env list 等只读探查不再占预算。
2. **路径守卫误杀合法命令**：`ls` 查看 conda env、R 库目录被"未授权路径"拦截，连输入目录 `ls -R` 也被拦，`R/3.6.0`、`/$v` 被误判成路径。现在**只读命令不限制路径**（环境探查是流程准备的合法一环）；递归发现（`ls -R`）在 RUN 目录与已选输入/参考目录内放开；`locate`/`tree` 仍禁；改变状态的遍历命令仍受路径授权约束；模块名/变量不再误判为路径。
3. **缺环境时干探而不是走部署通道**：执行器提示词新增环境处理规则——禁止逐个版本探软件；缺包 2 条命令内解决不了就把流程标记为 blocked_env 并写清缺失项，由用户一键部署，而不是无限试探。
4. **DSML 标记泄漏到对话**：模型（工具调用失配时）会把 `<｜｜DSML｜｜…>` 工具标记写进正文。现在服务端输出与前端渲染两侧都会剥离（历史消息同样生效），对话里不再出现这类原始标记。

## 验证

- TypeScript 静态检查通过（0 错误）。
- 全量自动化测试通过（只读探查放开、递归授权、预算只计非只读、DSML 剥离等用例已同步更新）。
