# HPClaw v0.4.5 流程图与对话大卡说明

发布日期：2026-09-17

## 流程图：一眼看清任务走到哪一步

- 新增流程图组件：把流程步骤渲染成一条横向节点链（步骤号 + 名称 + 状态图标），当前执行中的步骤脉冲高亮；完成=绿、失败=红、跳过=灰虚线、待执行=中性。
- 流程页新增"流程预览"区：运行前就能看到整条步骤链长什么样。
- 运行记录（流程页和对话内运行卡）展开后顶部即流程图，配原有的步骤时间线一起用。

## 执行过程合并成一张大卡

- 同一轮（两条用户消息之间）的所有命令/工具调用/计划记录合并为一张"执行过程"大卡片（默认折叠，点击展开），不再出现一排零散小卡片；卡片插在该轮首个执行项的位置，保持"解说→过程→结果"的阅读顺序。

## 验证

- TypeScript 静态检查通过（0 错误）。
- 全量自动化测试通过（新增流程图渲染/当前步高亮/点击联动、按轮合并大卡等用例）。
