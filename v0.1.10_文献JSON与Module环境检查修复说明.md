# HPClaw v0.1.10 文献 JSON 与 Module 环境检查修复说明

生成时间：2026-08-15

## 修复一：文献学习返回的流程 JSON 无法解析

### 原因

旧版只接受严格 JSON。AI 返回内容只要带有思考标签、Markdown 代码块、中文引号、尾逗号、字符串中的原始换行，或因为输出长度限制而缺少结尾括号，就会直接报“AI 返回的流程 JSON 无法解析”。

### 修改

- 提高文献学习的输出长度上限，并记录模型是否因为长度限制提前停止。
- 依次执行严格解析、常见格式修复和截断 JSON 补全。
- 支持移除思考标签与代码块，修复中文引号、尾逗号、原始控制字符以及常见 Python 布尔值写法。
- 本地修复仍不能形成有效流程时，只让 AI 修复 JSON 语法一次，不允许改变步骤含义或虚构论文内容。
- 只有得到包含有效步骤的流程对象后才写入流程库；仍失败时返回更明确的错误原因。

## 修复二：集群软件全部显示“没有找到环境”

### 原因

旧版在非交互 SSH 命令中直接调用 `module`，没有初始化集群的 Environment Modules/Lmod；同时只用一次精确文本查找判断模块是否存在。部分模块还依赖前置模块。修复初版中，`module load` 曾位于命令替换子进程，导致加载后的 PATH 没有保留，这也是“列表中有模块、版本命令却找不到”的直接原因。

### 修改

- 在非交互 shell 中自动初始化常见的 Environment Modules/Lmod 初始化脚本。
- 先验证模块系统状态，再区分“模块系统未初始化”“指定版本不存在”“模块存在但加载失败”和“版本命令失败”。
- 使用 `module avail`，在 Lmod 可用时补充 `module spider` 查找；兼容当前集群使用的 Environment Modules。
- 在同一个隔离 shell 中直接加载前置模块和目标模块，确保 PATH、LD_LIBRARY_PATH 等环境变化保留到版本检查。
- 流程软件配置支持 `prerequisiteModules`，例如 kallisto 先加载 `GCCcore/6.4.0`。
- 页面显示 Module 系统状态和每个软件的真实检查详情，环境检查等待时间提高到 120 秒。

## 真实集群验证

通过 HPClaw 当前保存的真实 LSF 集群连接执行预检：

- 调度器：LSF
- Module 系统：已初始化
- FastQC：`FastQC/0.11.9` 可加载
- Trimmomatic：`Trimmomatic/0.32-Java-1.8.0_92` 可加载
- kallisto：先加载 `GCCcore/6.4.0`，再加载 `kallisto/0.48.0`，版本命令返回 `kallisto, version 0.48.0`
- 最终预检状态：就绪

参考文件路径仍由用户在运行参数中指定，因此未指定时显示为可补充项，不会再把已安装的软件误判为缺失。

## 自动验证

- 文献 JSON 解析、流程接口和 Module 预检专项测试：38 项通过。
- 完整回归测试：84 个测试文件、576 项测试全部通过。
- TypeScript 类型检查通过。
- 前端正式构建和桌面服务端构建通过。
