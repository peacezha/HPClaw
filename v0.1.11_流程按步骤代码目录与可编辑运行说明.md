# HPClaw v0.1.11 流程按步骤代码目录与可编辑运行说明

生成时间：2026-08-15

## 目标

每次流程运行不仅保留状态和日志，还要把真正使用的代码按步骤保存下来，让用户可以查看、修改、复用和审计。

## 新的 RUN 目录

每次点击“运行流程”后，服务端会先创建独立 RUN：

```text
RUN/
├─ code/
│  ├─ README.md
│  ├─ step-01.sh
│  ├─ step-02.sh
│  └─ ...
├─ logs/
├─ results/
├─ config.json
├─ workflow.snapshot.json
├─ workspace.json
└─ run.json
```

- 一个流程步骤固定对应一个 `code/step-NN.sh`。
- 创建 RUN 时使用本次参数、参考路径和步骤命令覆盖渲染脚本。
- 尚未填写的 `{{PARAM}}` 会在脚本头标记 `HPCLAW_REVIEW_REQUIRED`，不会被悄悄忽略。
- `run.json` 保存 `codeDir`、每一步的 `scriptPath`、更新时间和用户修改标记。

## 页面查看和修改

展开任务监控中的某次运行，点击“查看/修改代码”：

- 左侧按流程步骤列出脚本。
- 右侧查看和编辑当前步骤的完整 shell 代码。
- 保存时使用临时文件加原子替换，并恢复可执行权限。
- 用户保存后步骤显示“用户已修改”，Agent 不得擅自覆盖。
- 保存只影响尚未提交的步骤或失败后的重试；已经提交到 LSF/Slurm 的作业不会被中途改写。

## Agent 执行规则

- Agent 开始步骤前必须读取 `run.json` 中当前步骤的 `scriptPath`。
- 真正计算从 `bash code/step-NN.sh` 或 `bsub < code/step-NN.sh` 发起。
- 长计算命令不再只存在于对话或临时终端命令中。
- `scriptUserModified=true` 时用户脚本优先；如与流程要求冲突，Agent 必须询问用户。
- 只读取当前步骤脚本，不扫描主目录、其他流程或其他 RUN。

## 安全边界

- 代码接口只能访问当前用户 `~/hpclaw_flows/*/03_workspace/runs/*/code/step-NN.sh`。
- 不接受 `..`、跨 RUN 路径或任意文件路径。
- 单个脚本最多 200 KB，空脚本不能保存。
- 手动从终端运行脚本不会伪造 `run.json` 状态；正式状态仍由流程状态机维护。

## 验证结果

- 完整回归：84 个测试文件、579 项测试全部通过。
- TypeScript 类型检查、前端正式构建和桌面服务端构建通过。
- 真实集群创建“集群作业排查流程”测试 RUN：自动生成 `code/README.md` 和 5 个 `step-NN.sh`。
- 步骤 1 的 `{{JOBID}}` 已替换为本次参数，页面编辑保存后再次读取内容一致，`scriptUserModified=true`。
- 页面已验证可按 5 个步骤切换、显示代码目录、编辑脚本和保存。
- 测试 RUN 最终标记为 cancelled；没有提交任何 LSF 作业，代码目录保留。
