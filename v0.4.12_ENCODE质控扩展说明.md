# HPClaw v0.4.12 ENCODE 质控扩展（SPOT/RPKM 轨迹/峰快照）说明

发布日期：2026-09-19

## 背景

v0.4.10 把 SPOT 加进了 ATAC-seq 并为 ATAC/DNase 做了植物叶绿体适配。本次把同样的 QC 理念推广到 ChIP-seq，
并补上信号轨迹与峰级可视化——这些都属于 **HPClaw 在 ENCODE 金标准基线上的有据改进**，理由已写入
`docs/ENCODE生物信息分析与质控流程.md` 的"HPClaw 在 ENCODE 基线上的改进"一节。

## 改动内容

### ChIP-seq（`encode-chipseq-tf` / `encode-chipseq-histone`，各 8 → 9 步）

新增第 7 步"QC：信号轨迹（RPKM bigWig）、SPOT 与峰快照"（位于峰调用之后、IDR 之前）：

- **bam → bigWig，RPKM 标准化**：`bamCoverage --normalizeUsing RPKM`，产出 `qc/tracks/*.rpkm.bw`，可直接拖进 IGV/基因组浏览器。
- **SPOT**：集群有 hotspot2 时自动计算（chrom.sizes 从 BAM 头生成），写入 `qc/spot.tsv`；缺失时明确 SKIP 不阻断。
- **IGV 风格峰快照**：固定随机种子（42）随机抽 5 个峰，画峰区 ±1.5kb 覆盖深度图（峰区高亮）到 `qc/igv/*.png`——在无图形界面的集群上也能抽查峰质量；需 matplotlib，缺失自动 SKIP。

软件清单相应补 deepTools（必需，钉 3.5.1）与 hotspot2（可选），环境检查步骤会自动覆盖这两项；QC 关卡里 FRiP/IDR 的 afterStep 随插入步骤顺延（7 → 8）。

### ATAC-seq（`encode-atacseq` 第 6 步）

- bigWig 从 CPM 改为 **RPKM** 标准化，产物归拢到 `qc/tracks/*.rpkm.bw`；
- 同样的 IGV 风格峰快照（`qc/igv/*.png`，数据源为 `*.final.bam` + MACS2 narrowPeak）。

## 验证

- TypeScript 静态检查 0 错误。
- 改动步骤脚本逐条过 `bash -n`，内嵌 Python（SPOT 汇总/峰快照）编译通过。
- 全量自动化测试通过（chipseq 步数断言同步为 9）。
