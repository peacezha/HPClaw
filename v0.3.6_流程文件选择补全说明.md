# HPClaw v0.3.6 流程文件选择补全说明

发布日期：2026-09-15

## 解决的问题

流程配置输入时只提供"选择文件夹"，没有选具体文件的操作；样本列表、GTF 注释、kallisto 索引等文件型参数只能手工输入路径。

## 更新内容

- 文件选取通道泛化为三种模式：选文件 / 选目录 / 文件或目录均可。
- 流程参数的 path 型字段改为「选择文件」+「选择目录」两个按钮，按参数需要各取所需；数据选择卡入口改为「选择文件或目录」。
- 选取模式底部确认栏按模式收紧：选文件时只有选中文件可确认，选目录时只有文件夹可确认，避免选错类型。
- 选取模式下双击文件直接确认选中（不再弹预览）；左侧文件树的选取模式同步支持选文件（旧行为选文件会退化为父目录）。
- 对话内流程配置卡与流程主页面两处入口行为一致。

## 验证

- TypeScript 静态检查通过（0 错误）。
- 全量自动化测试通过：130 个测试文件，共 912 项测试（新增选取确认栏、参数双按钮、文件树选文件等 13 项）。
