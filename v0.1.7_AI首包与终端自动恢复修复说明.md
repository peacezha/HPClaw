# HPClaw v0.1.7：AI 首包与终端自动恢复

## 现场根因

- 一条只有 9 个字符的提问，在调用 DeepSeek 前的上下文整理阶段耗时 146.6 秒。
- Agent 每轮都同步扫描集群技能、查询目录/作业/配额，并重建 1958 项技能关系；同步工作阻塞了 SSE 心跳，前端 90 秒后判定断流。
- 技能列表启动时把约 40 MB 完整正文一次传给界面，造成额外内存占用和卡顿。
- 交互式 Shell 把本地 Writable 的 `finish` 事件误判为远端终端死亡，且异常后只提示手动重连。

## 本版修复

1. AI 请求关键路径不再等待任何远程 SSH 扫描：只读取缓存，集群快照限时在后台更新。
2. 远程技能扫描从每 45 秒改为 5 分钟缓存，单文件最多读取 16 KB、总量最多 2 MB、命令硬上限 2.5 秒，并采用过期缓存先返回、后台刷新。
3. 本地技能索引驻留内存；远程技能只在内存合并，不再触发整库重建或写入本地大索引。
4. 技能图使用预解析关系和常数时间别名索引；普通问候不再误挂载无关技能手册。
5. 技能库列表只加载名称、简介、分类和大小；展开单项时再按需读取正文。
6. Shell 生命周期只以远端 `end/close` 为准；通道异常时自动重建 PTY，主 SSH 连接不退出。
7. AI 日志增加上下文耗时、缓存技能数和快照命中情况，能直接分辨卡在上下文还是模型供应商。

## 实测

- 1958+ 项技能下，普通问候上下文从约 1.8 万字符降到 474 字符。
- 上下文构建耗时约 65 ms。
- 使用软件现有 DeepSeek `deepseek-v4-pro` 配置进行真实 Agent 问答：约 5.5 秒完成，无工具误调用、无错误。
- 使用 v0.1.7 免安装软件连接真实集群后，10 字符简单问题的上下文耗时 110 ms，模型 2.44 秒开始响应，4.07 秒完整结束，`aborted=false`。
- 后台集群快照耗时 2.10 秒，但与 AI 请求并行，没有阻塞模型响应。
- 主动执行 `exit` 结束交互式 Shell 后，主 SSH 连接保持在线，软件自动重建 PTY；重建后的终端继续成功执行命令并返回提示符。
- 完整检查通过：81 个测试文件、559 项测试全部通过，代码规范检查通过。
