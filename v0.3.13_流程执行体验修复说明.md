# HPClaw v0.3.13 流程执行体验修复说明

发布日期：2026-09-16

## 流程不再频繁撞"12 条命令安全预算"

- 根因：正式流程的命令预算计数在整个 agent 调用上累计（含自动续跑各轮），多步骤流程总共只能跑 12 条命令就停下问"请确认是否继续"。
- 修复：预算改为**按续跑轮重置**——每轮最多 12 条命令，配合最多 3 次自动续跑与作业完成唤醒续跑，流程可以一口气跑完；防死循环保护（同命令重复、已成功变更、连续失败）保持全局不重置。普通对话的命令预算语义不变（用户设置）。

## AI 反问选项更智能

- 是非问（"是否继续？")→「是，继续 / 否，先不做 / 先暂停任务」。
- 问题里列了候选（引号词、"A、B 或 C"、"A 还是 B"、队列名等）→ 直接抽出来作为可点选项。
- 数值类问题（核数/内存/天数）→ 抽取文本中的数字候选，或给常用档位 + 自定义输入。
- 路径/失败/确认类选项文案保留并统一；要求模型反问时必须给 2-6 个贴合问题的具体选项。

## 流程运行自动独立对话，不再串对话

- 启动（或恢复）流程运行时，**自动新建专属对话**（标题自动取"流程：<流程名>")，运行协议与 AI 执行过程都写进专属对话，日常聊天不再被流程内容混入；每个流程、每次运行各自独立。
- 流程页、对话内流程配置卡、对话内嵌流程页三个运行入口行为一致；环境补齐、运行卡续跑仍留在当前对话。
- 作业完成唤醒的 AI 收尾也落在专属对话。

## 验证

- TypeScript 静态检查通过（0 错误）。
- 全量自动化测试通过：152 个测试文件，共 1105 项测试（新增预算按轮重置、选项抽取、专属对话标题派生等 18 项）。
