# HPClaw v0.2.20 修复说明

## 问题

集群账号认证失败或 SSH 会话失效时，文件传输接口可能返回：

```json
{
  "error": {
    "code": "SSH_SESSION_REQUIRED",
    "message": "An active SSH session is required"
  }
}
```

旧版将 `error` 对象直接保存到文件面板状态，并通过 `<p>{error}</p>` 渲染，触发 React #31，随后由全局错误边界显示“界面遇到异常”。后台服务和集群作业没有停止。

## 修复

- 新增统一错误规范化函数，支持字符串、`Error`、`{code,message}` 和多层 `{error:{code,message}}`。
- 文件传输 API 遇到非 2xx 响应时抛出标准 `Error`，同时保留 `code` 和 HTTP `status`。
- 文件列表、搜索和传输准备入口统一只向 React 状态写入字符串。
- 文件面板最终渲染处增加防御性转换，即使第三方或旧缓存意外传入对象也不会再次白屏。
- 新增 API 错误对象和组件渲染回归测试。
