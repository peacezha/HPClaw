# HPClaw v0.3.9 顶部导航与展示完善说明

发布日期：2026-09-16

## 导航移到顶部，侧栏空间释放

- 四个主导航（对话 / 计算资源 / 文件 / 流程）移到窗口顶部横排，左侧栏整个高度留给二级内容（对话记录、计算目标、文件树、流程列表）。
- 图标按钮补文字说明：顶部"主题 / QQ / 更新"，对话标题栏"技能库 / 新对话 / 保存 / 设置"等，不再只有图标让人猜。

## 文件右键菜单不再被窗口裁掉

- 文件传输与侧边文件树的右键菜单自动收拢到窗口内（靠右/靠底时自动左移/上移），超高时内部滚动，不再出现菜单显示不全。

## 对话记录显示集群对话

- 对话记录合并展示保存在计算资源上的对话（带来源徽标，本地优先去重），点击远程对话自动导入并打开；QQ 机器人写入集群的对话也能看到了。

## 富文本展示

- 图片放大：对话内所有图片（富内容卡片、Markdown 内联图、spec 卡片图）点击打开全屏查看器，支持适应视口/原始尺寸切换、Esc/背景点击关闭。
- 表格表头与卡片对比度提升：表头背景提亮、文字加粗变白，单元格与辅助文字整体提高一档，不再灰蒙蒙。
- 新增"示例：富文本结果展示"对话（首次启动自动出现，可删除）：演示表格、内嵌图片、网页链接与侧边打开的正确效果。
- 修复 `./` 开头相对路径被重复出卡片的小问题。

## 验证

- TypeScript 静态检查通过（0 错误）。
- 全量自动化测试通过：999 项测试（新增顶部导航、菜单收拢、lightbox、集群对话端点、示例种子、路径提取等用例）。
- 真实页面走查：顶部导航、示例对话的表格/图片/链接、图片全屏放大、表头对比度。
