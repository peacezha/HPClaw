# HPClaw v0.3.1 AI 工作台外壳重构说明

发布日期：2026-09-12

## 为什么重新调整

v0.3.0 在旧集群操作壳上增加了对话、文件和流程入口，但集群标签条及集群控制台仍然占据主导航，未能真正体现“对话为中心、集群为后台算力”的产品结构。

## v0.3.1 结构调整

- 删除主界面顶部的旧 Header、集群标签条和“AI 工作台 / 集群控制台”页面级切换。
- 默认主界面固定为 AI 工作台，顶部只展示当前任务上下文与后台计算目标。
- 左侧重构为 Codex 风格任务侧栏：新任务、对话、文件、流程和历史记录。
- 左下角新增“算力后台”入口，显示当前执行目标与已连接集群数量。
- 集群连接、集群切换、AI 集群执行权限、终端、作业和文件传输统一收纳到右侧算力后台抽屉。
- 本地 AI 与各集群成为“计算目标”，不再成为不同的对话标签页。
- 切换计算目标时，当前对话、上下文和流程状态保持不变。
- AI 对话栏改为轻量标题栏；输入框使用居中、圆角的任务输入样式。
- 首次 AI 配置改为居中的工作台引导卡片。

## 验证

- 在本地浏览器验证旧“集群控制台”入口数量为 0。
- 验证“新任务”和“算力后台”入口存在并可交互。
- 验证算力后台可以打开资源、终端和作业三个区域。
- TypeScript 静态检查通过。
- 全量自动化测试通过：118 个测试文件，共 803 项测试。

