# HPClaw v0.1.9：AI 流程持续执行与防提前退出

## 现场根因

真实日志显示 SSH 始终连接，AI 请求也以 `done` 正常收尾；故障并非网络断开，而是两个提前结束条件叠加：

1. 模型在正式流程仍为 `running` 时输出中间汇报，AI SDK 随即把这一轮当作最终答案结束。
2. LSF 查询返回 `Job <12345> is not found` 时，旧规则只看到 `Job <12345>`，错误地当成“新作业已提交”，把流程切到 `waiting_jobs` 并结束对话。

因此界面表现为 AI 停在第一步、进入后台监控或像“自己断开”，但实际 SSH 连接没有中断。

## 修复

- 正式流程每轮模型结束后，服务端都精确读取当前 RUN 的 `run.json`，不信任模型文字里的“完成”。
- run 状态仍为 `running` 时，中间文字不会作为最终答复显示；服务端会在同一个 SSE 请求内自动续跑。
- 续跑上下文包含权威运行状态和本轮真实命令证据，避免重复执行已经验证过的命令。
- 只有 `done / failed / cancelled / blocked_env / waiting_user / waiting_jobs` 或明确 `ask_user` 才允许结束。
- 达到命令、模型步骤或续跑安全上限时，运行会明确进入 `waiting_user`，并显示暂停原因，不再伪装成完成或断流。
- 正式 Agent 请求硬上限由 5 分钟延长到 15 分钟；首包和空闲超时仍保留，SSH、终端通道与后台作业生命周期继续独立。
- 即使模型没有生成最终文字，服务端也会根据权威状态生成明确的“已完成 / 等待作业 / 等待用户 / 失败”说明。
- LSF 只有明确返回 `Job <编号> is submitted` 才会登记为新提交作业；`is not found` 不再触发后台监控。
- 内置“集群作业排查流程”改为只接收 JOBID，不要求选择数据目录；5 个步骤只查询该作业，不扫描用户主目录、日志通配符或其他流程。

## 回归场景

新增自动化场景：

- 第一轮模型执行命令后只输出“下一步再继续”，但运行状态仍为 `running`。服务端不发送最终完成事件，自动发起第二轮；第二轮更新流程为 `done` 后才向界面返回最终答案。
- `bjobs -l 12345` 返回 `Job <12345> is not found` 时，不产生 `[JOB_SUBMITTED]`，也不进入 `waiting_jobs`。

2026-08-08 使用 v0.1.9 免安装软件连接真实 LSF 集群复测：JOBID 12345 从第 1 步连续执行到第 5 步，最终状态 `done, step 5/5`，用时约 134 秒；界面无“AI 请求超时或连接中断”，SSH 保持在线，结果正确判定为作业不存在。完整代码通过 84 个测试文件、572 项测试、TypeScript 类型检查和正式服务端构建。
