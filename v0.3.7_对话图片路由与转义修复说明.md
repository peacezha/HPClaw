# HPClaw v0.3.7 对话图片路由与转义修复说明

发布日期：2026-09-15

## 解决的问题

v0.3.5 之后仍有用户反馈"图片在软件里没有渲染，只有文字"。用真实对话复现后定位到两个叠加的底层问题：

1. **路由只按会话判定**：会话绑定集群时，AI 在本地产出的图片（dsh 引擎始终在本地写文件）被错误地发到集群 SFTP 通道，必然 404，降级为路径文本。
2. **Markdown 转义吃掉反斜杠**：CommonMark 把图片目的地里的 `\` 当转义符（`\.`→`.`、`\_`→`_`），`C:\...\runtime\.dsh-vision-toolkit\...` 这类含"反斜杠 + 标点"的 Windows 路径被改坏，同样 404。

## 更新内容

- 文件路由改为按路径形态判定：Windows 绝对路径永远走本地通道；点前缀相对路径本地优先；Unix 路径有集群会话先集群、本地兜底；主通道失败后自动跨端点重试一次，两端都失败才降级为路径文本。Markdown 内联图片与富内容卡片同一条规则。
- Markdown 解析前对图片目的地中的 Windows 路径做反斜杠保护（翻倍后由转义处理还原），`\.`、`\_` 等序列不再被吃掉。
- 已作为 Markdown 图片目的地内联渲染的路径不再重复生成富内容卡片（同一张图不再显示两次）。
- 真实对话复现验证：含 8 张图片的历史对话全部内联渲染成功、零降级。

## 验证

- TypeScript 静态检查通过（0 错误）。
- 全量自动化测试通过（含 Windows 路径集群会话路由、反斜杠转义还原、跨端点重试、图片目的地去重等回归用例）。
- 本地文件服务端点三形态（绝对 / 相对 / Unix 化）真实请求冒烟通过。
