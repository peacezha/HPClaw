# HPClaw v0.4.4 侧栏稳定性与 Agent 工作纪律说明

发布日期：2026-09-17

## 侧栏切换不再上下跳动

- 板块标题行固定为单行高度，"新任务"收进标题行（原来只在对话区独占一个大按钮，切到对话时整个侧栏下移）；实测切换各板块时导航行与底部工具行坐标完全不变。
- 底部工具行（主题/资源/QQ/更新）改为与主导航一致的"图标在上、小字在下"网格排布，修复文字被挤成竖排的问题。

## 对话展示：结果为主，过程可折叠

- "执行过程"（命令/工具调用/计划）与"思考过程"恢复默认折叠，主线程突出结果与总结；需要审查时点击展开即可。历史消息全程可见（不再收起较早消息）不变。

## Agent 工作纪律增强（学习成熟智能体）

系统提示词新增 AGENT DISCIPLINE 段，补齐成熟 agent 的四条工作习惯：

- **观察-调整**：每次工具结果回来后，先对照目标评估"拿到了什么、还差什么"，再决定下一步；不看输出连发下一步被明确禁止。
- **失败恢复**：失败后先读真实报错、形成假设、换方法试一次；同一方法最多重试一次，绝不原样重复；连续两次失败停下来诊断环境。
- **完成前自检**：回答前对照用户最初的问题检查是否真的用真实证据回答了；没拿到就直说缺什么，不编造。
- **上下文复用**：对话里已有的事实直接引用，不重复调用拿同一份数据。

## 验证

- TypeScript 静态检查通过（0 错误）。
- 全量自动化测试通过（执行过程默认折叠断言、提示词新段落断言、侧栏结构断言已同步）。
- 真实页面走查：板块切换零跳动、底部工具横排正常。
