# HPClaw v0.3.11 作业后台监控与提醒说明

发布日期：2026-09-16

## 核心：作业提交后，监控不再停

- 作业监控跑在**服务端**（每 45 秒轮询队列），与你在前端切换到哪个视图、哪个对话完全无关；视图切换、窗口最小化都不会中断监控。
- **内置（legacy）引擎路径补齐作业绑定**：此前只有 dsh 引擎会在提交作业后登记监控，非 DeepSeek 提供商/原生引擎/竞赛版下"提交即结束、无人唤醒"。现在两条引擎路径的作业完成后都会自动唤醒 AI 回到对话收尾（正式流程仍走独立续跑闭环，不重复唤醒）。
- **重启不丢监控**：应用重启并重新连接后，未唤醒的作业绑定自动恢复跟踪；关机期间结束的作业在首轮轮询即检出，照常发事件并唤醒一次。

## 作业结束时的提醒（多层）

- **对话内**：AI 被唤醒自动总结作业结果写回对话；事件指向非当前对话时，"对话"导航出现未读角标，点击即达。
- **桌面通知**：作业结束（DONE/EXIT）与 AI 续跑都会弹系统通知（Windows toast），窗口在后台也收得到，点击回到作业面板。
- **应用内 toast**：右下角浮出完成卡片（状态/摘要首行），点击直达作业面板；"计算资源"导航同步未读角标。
- **飞书/邮件推送**：作业面板 → 通知设置，填入飞书群机器人 webhook 即可（该功能此前已内置，本次在列表顶部加了配置引导提示）。

## 主页面看运行结果

- 作业完成事件现在带**输出摘要**（自动抓取 bpeek 尾部 30 行），作业面板"完成事件"列表点击即可展开查看，不用再去翻日志文件。

## 流程列表更紧凑

- 侧边流程列表只显示名称一行，描述移到悬停提示，浏览查找更快；管理面板详情不变。

## 验证

- TypeScript 静态检查通过（0 错误）。
- 全量自动化测试通过：151 个测试文件，共 1085 项测试（新增 legacy 绑定/唤醒、重启恢复、事件摘要、通知中心、toast、作业面板展开等 61 项）。
