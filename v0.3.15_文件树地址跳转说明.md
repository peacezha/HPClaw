# HPClaw v0.3.15 文件树地址跳转说明

发布日期：2026-09-16

## 更新内容

- 侧边"文件"导航的集群文件树新增**地址栏**：直接输入或粘贴路径回车即跳转——支持绝对路径、`~` 家目录前缀和相对路径；目录会逐级展开祖先链并定位，文件会选中文件本身并滚动到可见位置。路径不存在或不在家目录下时给出明确提示。
- 顺带修复一个潜在崩溃：文件树遇到 symlink 环（目录成为自身祖先）时会无限递归，已加循环保护。

## 验证

- TypeScript 静态检查通过（0 错误）。
- 全量自动化测试通过（新增嵌套目录/文件/~ 前缀/不存在/越界五类跳转用例）。
