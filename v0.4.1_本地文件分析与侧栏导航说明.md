# HPClaw v0.4.1 本地文件分析与侧栏导航说明

发布日期：2026-09-17

## 无集群也能分析本地文件

- 没有连接计算资源时，AI 不再只会纯聊天：本地模式自动获得一组**本地工作区工具**——列目录、读文件（分页/大小上限）、新建文件、在工作区里跑命令（cmd/PowerShell 环境）。把本地数据目录设为工作区后，可以直接让 AI 读 FASTQ/表格/日志并产出处理结果。
- 前端在无集群且未填工作区时，输入框上方会明确提示"本地文件分析需要指定工作区目录"。

## 安全策略（硬性）

- **工作区必填**：不设置本地工作区路径，本地工具一律拒绝并提示如何设置。
- **只能新建，不能删改**：AI 写文件只允许新建（已存在即拒绝"文件已存在，不允许覆盖"），没有删除/重命名/覆盖任何入口。
- **不能乱看**：读写全部限制在工作区内——`..`、绝对路径、UNC、符号链接/junction 越界一律拒绝（realpath 校验）。
- 命令执行有危险命令黑名单硬拒绝（rm/del/format/shutdown/Remove-Item 等），风险命令按既有确认策略弹确认。

## 导航回到侧边栏

- 五个主导航（对话/计算资源/文件/流程/数据资源）回到左侧栏大条目（选中高亮+指示条、徽标保留），顶部横条撤掉，界面纵向空间释放；主题/QQ/更新带文字标签在侧栏底部。

## 图标修正

- 流程运行页头部的装饰性"播放"三角换成流程语义的 GitBranch 图标；"未归属流程的运行"分组标题换 Activity 图标；侧边流程列表项悬停时出现真正的"配置并运行"按钮——三角形图标现在有真实功能了。

## 验证

- TypeScript 静态检查通过（0 错误）。
- 全量自动化测试通过：159 个测试文件，共 1218 项测试（新增工作区隔离/逃逸拒绝/只许新建/黑名单/本地工具组/导航与图标等 48 项）。
- 真实页面走查：侧栏导航与本地工作区提示显示正确。
