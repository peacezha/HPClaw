# HPClaw v0.4.9 文件重命名划选修复与终端右键菜单说明

发布日期：2026-09-17

## 文件传输：重命名划选不再误触拖拽

- 修复：重命名文件时用鼠标划选文件名文字，此前会误触发"拖拽文件传输"。现在重命名输入框激活时该行禁用拖拽，输入框内划选正常。

## 作业结束后 AI 自动分析（修复唤醒被反问卡住）

- 修复后台自动续跑被反问卡死的问题：此前 AI 唤醒后如果反问用户（ask_user），无人应答就一直挂起，分析结果永远写不进对话。现在唤醒提示明确禁止反问——AI 自主分析作业输出并收尾，需要拍板的地方按推荐方案执行并在总结里注明假设。

## 终端右键菜单 + AI 辅助小窗

- **右键菜单**（集群页面此前没有）：复制（有选区时）、粘贴、全选、清屏，以及"AI 辅助"开关（记住选择）。
- **AI 辅助**：开启后划选任何内容，右下角浮出小窗——即时本地分析（不联网不等 AI）：识别出路径给"进入目录/查看内容/解压"、识别出作业号给"查看作业/看输出/终止作业"、识别出报错给"发给 AI 深度分析"；下方自由输入框用自然语言说操作（如"解压这个文件"、"终止这个任务"、"跳到这个目录"），回车立刻在终端执行；识别不了的意图自动转交 AI。

## 验证

- TypeScript 静态检查通过（0 错误）。
- 全量自动化测试通过：168 个测试文件，共 1296 项测试（新增终端辅助分析/意图映射/右键菜单/重命名划选等 35 项）。
