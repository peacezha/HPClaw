# HPClaw v0.3.3 集群主界面与深色专业 UI 更新说明

发布日期：2026-09-15

## 集群控制台回到主界面

- 集群终端与作业监控从右侧 760px 抽屉搬回主界面整页：对话 / 流程 / 集群三个视图在主区一键整页切换，终端始终保持挂载保活，切换视图不打断 AI 后台执行。
- 集群页顶部恢复多集群标签条：点击切换、拖拽排序、悬停断开、末尾添加集群。
- 作业面板作为集群页右侧栏（可折叠），与终端同屏。
- 算力后台抽屉只保留资源管理（执行目标切换、AI 集群执行开关、连接集群、文件传输入口），从集群页工具条“资源管理”进入。

## 深色专业工具风 UI

- 默认主题改为深色：中性蓝灰面板、单一工程蓝强调色，替代原金色学术风。
- 去除 Playfair 衬线品牌字，统一 Inter / JetBrains Mono；圆角与阴影全面收敛，气泡感减弱。
- 浅色主题保留并同步中性化（灰阶 + 同一蓝色），主题切换按钮不变，已存偏好不受影响。

## 交互修复

- 修复终端 Ctrl+C / Ctrl+V 经常复制粘贴两次的问题：xterm 对自定义按键处理器在 keyup 时也会回调，按住 Ctrl 先松 V 会再触发一次粘贴；现已只响应 keydown。
- 移除“快速思考”模式：该模式的执行协议在服务端被静默丢弃，导致只给建议不执行。聊天统一为 Agent 模式；终端选中文字“发给 AI 解读”改走 Agent 模式。终端命令自动补全不受影响。
- 文件传输本地面板默认从“此电脑”盘符列表开始，点击进入 C:\ / D:\ 等盘；盘符根目录下“上一级”可返回盘符列表；盘符层禁用新建与搜索。

## 流程可视化与富结果

- 对话内流程配置卡：AI 判断要运行流程但参数未齐时，对话中直接渲染表单卡片（下拉、数字、开关、文件/目录选择器），用户点点点配好后一键确认运行；输入框上方的“匹配流程”推荐点击后同样插入配置卡，不再只发文本。
- 对话内流程运行卡：流程运行的环境预检（缺项可一键安装部署）、步骤时间线、命令与输出、结果产物全部以卡片形式实时出现在对话里，默认紧凑一行进度、点击展开详情；产物目录可直接打开。
- 流程管理面板（新建 / 编辑 / AI 生成 / 文献学习）恢复入口：左侧“流程”页顶部“管理流程”。
- AI 答复支持 Markdown 表格（remark-gfm）；修复图片 / CSV 表格卡片因服务端接口缺失从不渲染的问题（新增 `/api/files/read`、`/api/files/read/batch`、`/api/files/view`，按集群会话路由）。
- 链接点击统一交给系统浏览器；新增侧边网页栏：AI 答复中的网页链接和集群生成的 report.html 都可以“在侧边打开”，右侧 600px 内嵌面板查看（外部网页走隔离 webview，集群报告走沙箱预览）。

## 验证

- TypeScript 静态检查通过（0 错误）。
- 全量自动化测试通过：126 个测试文件，共 853 项测试（新增终端粘贴、盘符列表、流程卡片、富内容、网页栏等 37 项）。
- 使用本地真实页面截图验证深色主题、浅色主题与连接集群弹窗观感。

## 已知事项

- 依赖备注：`remark-gfm` 为新增依赖；项目锁定的 `tailwind-merge@^4.1.0` 在公共 registry 不存在，完整 `npm install`/`npm ci` 会失败（v0.3.2 即如此），本次依赖已直接装入 node_modules 并登记 package.json。
