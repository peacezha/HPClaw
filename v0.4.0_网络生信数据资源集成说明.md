# HPClaw v0.4.0 网络生信数据资源集成说明

发布日期：2026-09-16

## 55 个生信公共数据 API 接入

新增"网络数据资源"能力：把生信领域主流公共数据库/网站的开放 API 集成进软件，可直接用于后续数据分析。

- **55 个服务，覆盖 10 大类**：基因与基因组（NCBI E-utilities、Ensembl、GO、QuickGO、MyGene、UCSC、JASPAR、OMA、HGNC、RNACentral、miRBase)、蛋白与结构（UniProt、InterPro、PDBe、RCSB、AlphaFold、PRIDE)、通路与互作（Reactome、KEGG、STRING、IntAct、ComplexPortal、Rhea)、化合物与药物（ChEMBL、PubChem、openFDA、Pharos、MetaboLights、ChEBI、MyChem)、变异与临床（Open Targets、cBioPortal、ClinGen、MyVariant、NLM Clinical Tables)、表达与单细胞（HPA、Expression Atlas、scRNA Atlas)、文献（Europe PMC、bioRxiv、CrossRef、OpenAlex、DOAJ、iCite)、物种与分类（MGI/FlyBase/WormBase/SGD 经 AGR)、植物（Gramene、TAIR、Phytozome、Plant Reactome)、微生物（MGnify、GTDB、BV-BRC)。
- **每个服务实测可用**：全部 55 个接口经真实网络逐一调用验证（55/55 通过）；探针带瞬时失败自动重试，页面可一键"全部测试"复查。
- **独立"数据资源"页面**（顶部导航第 5 项）：按类别浏览/搜索全部服务与端点，状态灯显示可用性，每个端点可"试一下"直接调用看返回。
- **对话即可调用**:Agent 新增 `search_web_apis`（找服务）与 `call_web_api`（调接口）两个工具——直接用自然语言提问，例如"查 TP53 在 UniProt 里的功能注释"、"KEGG 里查糖酵解通路"、"用 STRING 查这个基因集的互作网络"、"ChEMBL 里查伊马替尼的靶点"，AI 会检索合适的数据库、调用接口并把结果整理成表格/要点回答。dsh 引擎经桥接同样可用。
- 调用层有 SSRF 防护（仅注册表内主机）、超时与大小上限、错误分类；复杂 Accept 头兼容性问题已实测修复（UniProt 等）。

## 验证

- TypeScript 静态检查通过（0 错误）。
- 全量自动化测试通过：158 个测试文件，共 1170+ 项测试。
- 真实网络全量探针：55/55 通过（个别服务偶发超时经自动重试通过，属上游正常波动）。
