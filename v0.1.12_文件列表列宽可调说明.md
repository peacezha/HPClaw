# HPClaw v0.1.12 文件列表列宽可调说明

生成时间：2026-08-15

## 修复的问题

文件传输页面原来采用固定表格布局，“名称、大小、修改日期、权限”会被压缩在面板宽度内。自动生成的长文件名只有末尾编号不同时，末尾编号经常被省略，用户难以快速找到正确文件。

## 新的操作方式

- “名称”列默认加宽，更适合查看流程生成的长文件名。
- 将鼠标放到任意两个表头之间，左右拖动分隔线即可调整该列宽度。
- 列宽增大超过当前面板后，文件区域会出现横向滚动，可继续查看完整文件名。
- 本地文件和集群文件的列宽分别记忆，软件重新打开后仍保留上次设置。
- 双击表头分隔线可恢复该列默认宽度。
- 键盘聚焦分隔线后可用左右方向键每次调整 20 像素。
- 文件名仍放不下时，鼠标悬停会显示完整名称。

## 验证

- 文件面板 17 项专项测试通过，包含鼠标拖动、键盘调整、恢复默认、列宽持久化和完整文件名提示。
- TypeScript 类型检查通过。
- 前端生产构建通过。
