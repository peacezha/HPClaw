# HPClaw v0.3.17 流程执行上限放宽说明

发布日期：2026-09-16

## 解决的问题

用户反馈"流程一个任务都没有完整跑完就断了"：正式流程的安全上限（48 模型步骤 / 最多 3 次自动续跑）对真实多步骤流程太短，每个流程步骤通常需要 5–10 个模型步骤，五步流程很容易在一半处被暂停，要求用户反复点"继续"。

## 更新内容

- 流程单次执行上限：**模型步骤 48 → 200**、**自动续跑 3 次 → 8 次**（每轮命令数仍为 12 条并按轮重置，最多 108 条命令）。绝大多数流程现在可以一轮完整跑完，不再中途暂停。
- 超出新上限仍暂停（那通常意味着任务真的卡住了），暂停语义与"点击继续从当前步骤恢复"不变；设置页说明文案同步更新。
- 作业提交到队列后的"作业完成唤醒续跑"链路不受影响，长作业依旧由后台监控接管。

## 验证

- TypeScript 静态检查通过（0 错误）。
- 全量自动化测试通过（预算配置与 8 次续跑暂停语义的断言已同步更新）。
