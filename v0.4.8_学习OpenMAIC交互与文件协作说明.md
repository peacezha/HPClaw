# HPClaw v0.4.8 学习 OpenMAIC 的交互与文件协作说明

发布日期：2026-09-17

本版本参照 [OpenMAIC](https://github.com/THU-MAIC/OpenMAIC)（清华 Open Multi-Agent Interactive Classroom）的**真实 UI 源码**做适配（不是看截图）：研读了它的 `workspace-shell.css`（语义 token、可测对比度分级、分层投影、真实焦点态）与 `workbench-chat.css` / `composer-pill.tsx`（聊天面板 token 系统、附件 pill 登记法）等源码文件。

## 对话内交互式 HTML 内容卡

- AI 答复里的 .html 文件（集群/本地工作区）与 ```html 围栏完整页面，原地渲染成**可交互卡片**（默认收起、展开即在沙箱里跑，脚本可开关，CSP 禁外网）——对应 OpenMAIC 的"交互式模拟页"模式。
- 卡片自带"在侧边打开"入口；普通代码块不受影响。
- Agent 提示词同步：交付交互图表/报告时写自包含 .html 或 html 围栏即可自动出卡。

## 对话附件上传（材料管线）

- 聊天输入框新增回形针附件按钮：本地模式把文件复制进工作区 `attachments/`（不覆盖），集群模式上传到集群 `~/hpclaw_uploads/`；附件以路径引用随消息发给 AI。
- 附件 chips 采用 quiet pill 样式（细边框、低饱和底、× 悬停显现）。

## 侧边网页栏多标签

- 侧边网页栏支持多标签：每次"在侧边打开"追加标签，点击切换、单个关闭、重复打开自动聚焦既有标签，全部关闭时面板收起。

## 视觉细节（照源码手法）

- 输入框聚焦态：accent 描边 + 轻微上浮 + 分层投影。
- 富内容卡片 hover 投影改分层（contact→mid→wide）。

## 验证

- TypeScript 静态检查通过（0 错误）。
- 全量自动化测试通过：165 个测试文件，共 1261+ 项测试。
- 真实页面截图确认输入框聚焦态与附件 pill。
