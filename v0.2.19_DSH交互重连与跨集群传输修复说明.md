# HPClaw v0.2.19 修复说明

本版本集中修复 DSH 交互阻塞、账号动态验证码串用、断网重连和跨集群文件传输可靠性问题。

## 1. DSH 反问不再超时

- 完整接入 DSH `question/requested` 双向协议，DSH 请求用户补充信息时会在当前 AI 对话中显示问题和选项。
- 用户作答后，答案通过原请求的 `rpcId` 回传，DSH 在同一个任务和上下文中继续执行。
- 等待用户作答期间暂停空闲超时，最长保留 14 分钟；取消请求时同步取消等待。
- 冷恢复历史 DSH 会话时先处理遗留反问，再提交新消息，避免只显示“DSH 接管”却没有后续内容。

## 2. 多账号 TOTP 隔离

- 密码和 TOTP 密钥严格按 `主机 + 端口 + 用户名` 匹配账号，不再让全局默认值覆盖已保存账号。
- 切换或手动修改账号身份时，立即清除上一账号的密码、TOTP 密钥和验证码。
- 只有没有任何账号配置时才兼容旧版全局凭据，避免历史用户升级后无法登录。

## 3. 断网后原会话可重连

- SSH 断开后保留会话记录，不再把重连所需上下文一并删除。
- “重新连接”按钮调用后端重连接口，重新建立 SSH、SFTP、终端、文件监听和技能环境。
- 每次重连都根据账号保存的 TOTP 密钥生成新验证码，绝不复用已失效的一次性验证码。
- 前端 Socket 在后端重连成功后重新绑定到新终端。

## 4. 文件路径按集群独立记忆

- 本地端和每个集群端点分别保存当前目录。
- A 集群切到 B 集群再切回 A 时，恢复 A 上次访问的目录，不再错误继承 B 的路径。

## 5. 跨集群传输权限预检

- 展开目录和创建传输任务之前先检查源路径可读、目标目录可写。
- 对当前账号拥有的源文件补充 `u+r`、源目录补充 `u+rx`，并给必要的父目录补充 `u+x`。
- 目标侧从最近存在的父目录开始检查并创建目标目录，确认写入和进入权限。
- 目标 SSH 会话已失效时立即返回错误，不再让权限预检请求永久等待。
- 同一主机的不同账号优先使用精确 POSIX ACL，只给目标账号文件读取权和必要父目录穿越权；不使用 `chmod 777`、`o+` 或全员授权。
- ACL 不可用时自动回退为双 SFTP 中继，由源账号读取、目标账号写入，不扩大文件权限。

## 验证要求

- TypeScript 类型检查通过。
- Vitest 全量测试通过。
- Web 前端与 Electron 后端均完成生产构建。
- NSIS 安装包通过内嵌 7z CRC 完整性校验，并执行静默安装、启动和接口烟测。
