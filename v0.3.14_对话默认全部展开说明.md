# HPClaw v0.3.14 对话默认全部展开说明

发布日期：2026-09-16

## 更新内容

按用户要求，对话内容默认全部可见，不再收起：

- **"执行过程"卡片默认展开**：Agent 的计划、步骤、工具调用、集群命令和命令输出全部直接显示，点击卡片标题仍可折叠。
- **"思考过程"默认展开**：推理步骤直接显示，可点击折叠。
- **较早消息不再收起**：此前超过 60 条消息时较早内容会被收起（需到对话存档查看），上限提高到 500 条，常规对话全程可见。

## 验证

- TypeScript 静态检查通过（0 错误）。
- 全量自动化测试通过：152 个测试文件，共 1105 项测试（执行过程卡片默认展开/点击折叠的行为断言已同步更新）。
