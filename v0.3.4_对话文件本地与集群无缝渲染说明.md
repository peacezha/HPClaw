# HPClaw v0.3.4 对话文件本地与集群无缝渲染说明

发布日期：2026-09-15

## 解决的问题

对话中 AI 提到的图片和文件，此前只有在集群上的才能内联显示；本地文件（如 AI 视觉工具、脚本产出的图片）只显示为一行文字路径。本版本打通本地与集群两条文件通道，对话中提到的文件无论在哪一端都能内联渲染。

## 更新内容

- 新增本地文件路由：`/api/local/files/read`、`/api/local/files/read/batch`、`/api/local/files/view`，允许根限定为应用数据目录（`%APPDATA%\HPClaw\runtime`）与当前 AI 工作区，realpath 校验阻断越界访问。
- 路径识别覆盖三种形态：Windows 绝对路径（`C:\...\x.png`）、工作区相对路径（`.dsh-vision-toolkit/artifacts/x.png`）、模型 Unix 化写法（`/artifacts/x.png`，带剥前缀探测与有界后缀搜索兜底）；同时修复长路径被 Unix 模式截断误抓的问题。
- AI 答复中 `validate_dsh_ui` 类 UI spec JSON 不再裸文本显示：自动识别并渲染为卡片（标题 + 表格 + 段落 + 图片 + 链接），正文与执行记录两处入口都接入。
- 本地图片卡片支持 view 流直接另存下载。

## 验证

- TypeScript 静态检查通过（0 错误）。
- 全量自动化测试通过：129 个测试文件，共 889 项测试（新增本地路由、路径提取、spec 解析与卡片等 36 项）。
- 真实 server 冒烟验证三种路径形态读取与 view 流、根外路径拒绝。
