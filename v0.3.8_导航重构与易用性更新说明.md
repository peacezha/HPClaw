# HPClaw v0.3.8 导航重构与易用性更新说明

发布日期：2026-09-16

## 主导航重构：板块摆到明面上

- 左侧改为清晰的两级导航：**对话 / 计算资源 / 文件 / 流程** 四个大条目始终可见（图标 + 文字 + 选中高亮），不再藏在小 tab 和底部按钮后面。
- "计算资源"二级区直接列出计算目标：本地 AI 工作台（标注"无需服务器，可本地分析"）+ 每个已连接资源（点击即切换），"添加计算资源"按钮和"资源管理"入口都在明面上。
- 流程列表按分类分组（可折叠），管理流程入口保留。

## "集群"更名为"计算资源"

- 全部用户可见文案统一为"计算资源"（连接计算资源、添加计算资源、AI 计算资源执行等），强调没有服务器也能用：本地 AI 即可完成分析与对话。

## 文件右键菜单

- 左侧文件树新增右键菜单：打开、复制路径、复制、剪切、粘贴、重命名、删除、下载到本地。
- 传输工作区面板补"剪切"（同端点剪切粘贴 = 移动）与 Ctrl+C / Ctrl+X / Ctrl+V 快捷键。

## 验证码改为明确选填

- 登录表单验证码标注"（选填）"，提示"服务器无二次验证可留空"；无 TOTP 秘钥时的红色报错降级为中性提示。（空验证码链路本就支持，此前是 UI 误导为必填。）

## 流程系统

- 流程按七大类分组：基因组与变异分析、转录组与表观调控、单细胞与免疫分析、蛋白代谢与多组学、微生物与病原分析、基因编辑与 CRISPR、任务管理与通用工具；管理面板可按分类筛选，编辑器可自选/自定义分类。
- 修复三个真实 bug：编辑内置流程重启后被静默回滚；删除内置流程重启后复活；AI 生成的流程保存后来源标签错标为"自定义"。
- 功能自查结论：新建/编辑/删除可用无外部依赖；AI 生成需配置 API Key；文献学习需 API Key + 外网（DOI 开放获取全文，或上传 PDF，扫描件 PDF 不支持）。

## 集群页排版修复

- 修复作业面板开合/窗口跨断点后终端不换行、右侧被裁或留白（终端容器 ResizeObserver 自动重排）。
- 修复窄窗口下终端被 340px 固定作业面板压没（窄屏改为遮罩式面板）。
- 修复首次切到集群页终端短暂错乱（0 尺寸下不再无条件初始化，尺寸就绪后再初始化）。
- 标签条横向滚动条变细；选区浮动工具条定位常量按 16px 字号修正。

## 验证

- TypeScript 静态检查通过（0 错误）。
- 全量自动化测试通过：133 个测试文件，共 966 项测试（新增导航、登录选填、右键菜单、剪切、终端重排、流程分类与存储迁移等 47 项）。
- 本地真实页面截图走查：新导航、计算资源二级区、流程分组、登录弹窗选填标注。
