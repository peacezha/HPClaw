# HPClaw v0.3.16 重连挂起修复说明

发布日期：2026-09-16

## 解决的问题

用户反馈"跑流程不动了，显示重连中"。排查确认：SSH 握手（密码 + 验证码）完成后，集群登录节点迟迟不放行 shell 通道（节点过载或家目录存储卡顿时会发生），而代码里通道建立阶段**没有任何超时**——`readyTimeout` 只覆盖握手阶段。于是重连永远停在"重连中"，流程监控也无法恢复。集群侧此时确实可能处于过载状态，但软件不该因此无声地永久等待。

## 更新内容

- **shell / SFTP 通道建立增加 60 秒超时**（首次连接与终端重建两处）：超时后以明确错误失败——"终端通道建立超时（60 秒无响应）：集群登录节点可能过载或存储卡顿，请稍后重试"，前端会显示可重试的失败状态，而不是永远"重连中"。
- 失败后可随时再次重连；连接恢复后，流程监控、作业绑定与作业轮询会按既有链路自动恢复。

## 遇到此错误时怎么办

这通常意味着集群登录节点正过载或存储卡顿：稍后重试，或联系集群管理员确认登录节点状态。软件本身会在 60 秒后给出明确提示而不是永久等待。

## 验证

- TypeScript 静态检查通过（0 错误）。
- 全量自动化测试通过（新增"节点不放行 shell 通道时 60 秒明确失败"与"终端重建超时"两个回归用例）。
