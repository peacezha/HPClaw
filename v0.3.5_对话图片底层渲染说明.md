# HPClaw v0.3.5 对话图片底层渲染说明

发布日期：2026-09-15

## 解决的问题

AI 想在答复里展示本地图片时，客户端没有底层渲染通道：此前一个真实会话中，AI 被迫自己编写并启动一个临时 HTTP 图片服务（约 20 次工具调用），再用 `![desc](http://127.0.0.1:8765/x.png)` 嵌入对话，服务重启即失效。本版本把"标准 Markdown 图片语法 + 普通文件路径"做成客户端原生能力。

## 更新内容

- Markdown 图片渲染接管：`![描述](路径)` 的 src 非 http(s) 时一律按文件路径处理——集群会话重写为 `/api/files/view`（SFTP），本地重写为 `/api/local/files/view`；Windows 绝对路径（含 `file://` 与 percent-encoding 还原）、工作区相对路径、Unix 绝对路径都可用，加载失败降级为路径文本，不留破图。
- react-markdown 的 URL 消毒白名单放行 `file:` 与 Windows 盘符路径（`javascript:` 等仍按默认消毒）。
- Agent 提示词矫正（原生引擎与 dsh 引擎双侧）：展示图片直接用标准 Markdown 图片语法写文件路径，客户端自动内联渲染；明确禁止为展示图片启动临时 HTTP 服务或 base64 内联；表格统一用标准 Markdown 表格语法。
- 网页图片点击可在侧边网页栏打开。

## 验证

- TypeScript 静态检查通过（0 错误）。
- 全量自动化测试通过：129 个测试文件，共 899 项测试（新增图片路径重写、URL 消毒、降级展示、提示词断言等 10 项）。
