# HPClaw v0.4.11 命令确认"信任全部"修复说明

发布日期：2026-09-19

## 问题

AI 请求执行命令时的确认弹窗有三个按钮：拒绝 / 信任 / 执行。用户反馈"信任"功能形同虚设——点了之后后续命令照样逐条弹窗。

## 根因

两层叠加，导致"信任"**从来没有真正生效过**：

1. 前端每发起一轮 AI 对话（`handleAgentMode` 开头）都会无条件 `setConfirmMode("ask")` 重置信任状态——即使信任成功设置过，下一轮（含流程自动续跑的新请求）立即失效。
2. 更深的 React 闭包陷阱：SSE 流式循环里判断 `confirmMode === 'trust_all'` 读的是**该轮渲染时的 state 快照**。流式循环运行期间组件重渲染不会更新旧闭包，所以点了"信任"之后，同一轮内的后续命令照样弹窗——信任连当轮都不生效。

## 修复

- **删除每轮开头强制重置 `ask` 的逻辑**：信任状态在轮次之间、流程自动续跑之间都保持。
- **SSE 事件循环改读 `confirmModeRef.current`**（ref 同步更新，不受闭包快照影响）：点"信任全部"当轮立即生效，后续命令不再弹窗。
- **信任状态持久化到 localStorage**（`hpclaw-confirm-mode`）：重启软件后仍然有效。
- **新增可见的取消入口**：信任激活时输入区上方常驻一行提示"已信任全部命令……取消信任"，随时一键恢复逐条确认。
- 按钮文案"信任"改为"**信任全部**"，并加悬停说明，语义不再含糊。

## 语义边界（保持不变）

"信任全部"只跳过确认弹窗；服务端硬性安全规则不受影响——`rm` 仍被禁止（用 `mv /tmp`）、正式流程的目录越界扫描仍被拦截、危险命令黑名单照常用。

## 验证

- TypeScript 静态检查 0 错误。
- 全量自动化测试通过：168 个测试文件，1296 项。
