# HPClaw v0.3.2 Agent 交互与 UI 层级整理说明

发布日期：2026-09-12

## 对话信息层级

- 主线程突出用户消息与 AI 最终答复。
- 连续的 Agent 计划、步骤、工具调用、集群命令和命令输出合并为一张“执行过程”卡片。
- 执行过程默认折叠，摘要显示步骤数、命令数和工具调用数；用户可按需展开审查完整命令与输出。
- 思考内容默认折叠，并兼容 `<thought>` 与结构化 `thoughtSteps` 两种来源。
- 快速模式不再把 `<execute>` 原始标记作为 AI 正文显示，实际命令改为执行过程记录。
- AI 最终答复使用正文式排版，降低多层气泡和边框造成的视觉噪声。

## 弹窗与设置

- 修复从算力后台打开集群登录时，登录表单被后台抽屉遮挡的问题。
- 打开集群登录前自动收起算力后台，并将登录窗口提升到独立的最高交互层。
- 集群登录改为紧凑表单，在 720p 高度下可完整显示主要字段和登录按钮。
- 登录窗口新增明确的关闭按钮。
- AI 设置改为独立右侧抽屉，不再向下挤压或遮断主对话。

## 左侧栏与文件树

- 修复文件树所在父级没有建立纵向弹性布局，导致大量文件撑出可视区域后被裁切、滚轮无法继续滚动的问题。
- 文件树现在独立占用左侧栏的剩余高度并纵向滚动，顶部导航和底部算力后台入口保持固定。
- 限制横向溢出并稳定预留滚动条空间，深层目录和长文件名不会挤动整个侧栏。

## 验证

- 使用本地真实页面验证集群登录窗口层级和完整可见性。
- 使用临时 Agent 会话验证执行过程默认折叠、展开后显示命令与输出、思考过程独立折叠。
- 验证完成后已删除临时会话和测试配置。
