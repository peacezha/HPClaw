# v0.2.18 DSH 空回答与错误配置修复说明

## 故障现象

AI 问答显示“dsh 引擎已接管，正在执行…”，随后结束，但界面没有回答正文，也没有显示失败原因。

## 根因

1. DSH 用户设置中的 `llm-deepseek.baseURL` 可能被误填为 API Key。该设置优先于 HPClaw 启动时注入的正确接口地址，导致模型请求以 `TRANSPORT` 错误结束。
2. HPClaw 将所有 DSH `turn/end` 事件都当作成功，不检查 `reason.kind=error`，因此真实失败被翻译成空的 `done`。
3. HPClaw 原先先提交 prompt、再建立 DSH mux 订阅；极快的回答存在漏掉正文事件的竞态。

## 修复内容

- DeepSeek 自定义地址必须是合法的 HTTP(S) URL，禁止把 API Key 或带凭据的 URL 当成接口地址。
- DSH 启动前仅移除无效的 `llm-deepseek.baseURL`，保留其他用户设置；有效的自定义网关不受影响。
- DSH 错误终态现在发送 `error`，空的成功终态也会显示明确错误。
- 错误日志和界面消息会遮蔽 `sk-...`、Bearer Token 和 URL 内嵌凭据。
- 普通 AI 问答和作业自动续跑均改为先连接 mux，再提交 prompt。
- 新增地址修复、错误翻译、脱敏、空回答及快速响应顺序测试。

## 验证结果

- DSH 定向测试：33 项通过。
- 项目全量测试：109 个测试文件、776 项测试通过。
- TypeScript 类型检查通过。
- Vite 前端生产构建与 Electron 服务端构建通过。
- 清理本机错误设置后，针对当前运行 DSH 的真实最小请求以 `completed` 结束并返回非空正文。
