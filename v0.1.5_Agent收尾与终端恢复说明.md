# HPClaw v0.1.5 Agent 收尾与终端恢复

## 修复的问题

- 运行流程后，运行抽屉遮住聊天区的命令确认和 AI 反问，导致界面长时间显示“正在生成”。
- Agent 在 `bsub` / `sbatch` 提交成功后继续循环查询调度器，占用聊天流并增加 SSH 压力。
- 终端 Shell 通道死亡时，“点击重连”只重连 WebSocket，没有在集群端重新创建 PTY。
- 模型供应商或确认工具异常等待时缺少可靠的服务端收尾上限。

## 新行为

1. 正式运行创建后自动关闭运行抽屉并切到聊天区，用户能立即看到命令确认或 AI 问题。
2. 检测到 LSF `Job <id>` 或 Slurm `Submitted batch job <id>` 后：
   - 自动登记 Job ID；
   - 正式流程进入 `waiting_jobs`（后台监控）；
   - 当前 Agent 计划步骤进入 `waiting`；
   - 立即结束本轮 Agent，不再循环执行 `bjobs` / `squeue`。
3. 后台监控检测到作业完成：
   - 全部步骤完成时，流程进入 `done`；
   - 仍有后续步骤时，流程进入 `waiting_user`，等待用户继续。
4. Shell 通道断开时，前端请求服务端在现有 SSH 连接上重新创建远程 PTY，并在成功后恢复终端尺寸和输入。
5. SSH 主连接断开与 Shell 通道断开分开提示；主连接断开时明确要求重新登录，同时说明已提交的调度器作业不会因此停止。
6. 单轮 Agent 增加 5 分钟服务端看门狗，命令确认最长等待 2 分钟，避免无限生成。

## 验证

- TypeScript 类型检查通过。
- 针对性 48 项测试通过。
- 完整测试通过；新增批处理自动交接、抽屉自动关闭、后台状态转换和 Shell 重建测试。
