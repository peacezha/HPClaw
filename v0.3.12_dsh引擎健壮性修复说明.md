# HPClaw v0.3.12 dsh 引擎健壮性修复说明

发布日期：2026-09-16

## 背景

用户反馈"dsh 运行不了"。经完整排查（sidecar 会话、模型选择、桥接鉴权、mux 帧逐级探测）确认：dsh 引擎本身健康，直接原因是 **DeepSeek 账户余额不足**（API 返回 402 Insufficient Balance / QUOTA），每次请求都在首次调用模型时失败。本次顺手修复排查中发现的两个健壮性问题。

## 更新内容

- **无集群会话时 dsh 请求不再硬报错**：此前本地 AI 模式（无 SSH 会话）下 dsh 引擎直接抛出 "dsh Agent requires an active SSH session"。现在静默回退到内置引擎完成本轮对话，不再向用户抛硬错误。
- **dsh 侧错误写入服务端日志**：QUOTA（余额不足）、引擎异常结束、空回答、stream error 等错误帧此前只发送到前端，服务端日志毫无痕迹（本次排查因此多花一倍时间）。现在同步写入 `hpclaw-server.log`，出问题时可直接在日志里看到真实原因（如 `Insufficient Balance`）。

## 遇到 "dsh 引擎执行失败（QUOTA）" 时

这不是软件故障：请检查 DeepSeek 账户余额（或更换 API Key）。错误文案中括号内为供应商错误码，其后为详细信息。

## 验证

- TypeScript 静态检查通过（0 错误）。
- 全量自动化测试通过（新增无会话回退、错误帧日志回归用例）。
- 对运行中的 dsh sidecar 实测：会话创建、deepseek-flash 模型选择、prompt 受理、mux 帧链路全部正常。
