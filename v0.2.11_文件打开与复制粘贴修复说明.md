# HPClaw v0.2.11 文件打开与复制粘贴修复

## 修复内容

1. 远程文件“打开并编辑”改为直接调用 Windows 默认应用关联，不再经过可能卡住的隐藏命令进程。
2. 编辑会话先显示“正在打开”；只有系统确认接收打开请求后才显示“编辑中”。默认应用缺失或打开失败时显示“打开失败”，可点击“重试打开”。
3. 再次打开已有本地缓存的远程文件时，仍经过编辑会话管理器，继续监听保存并自动回传集群。
4. 左右文件面板右键菜单新增“复制”和“粘贴”，同时支持文件和文件夹：
   - 本地目录内复制；
   - 同一集群目录内复制；
   - 本地与集群、两个集群之间复制传输；
   - 同名目标自动使用“副本”名称，不覆盖原项目；
   - 禁止把文件夹复制进自身子目录。
5. 多集群模式下，打开、预览和自动回传使用文件实际所在的集群会话，避免误用另一标签的连接。

## 验证

- TypeScript 类型检查通过。
- 全量自动化测试：100 个测试文件、721 项测试全部通过。
- 安装包构建时执行内嵌负载 CRC 校验。
