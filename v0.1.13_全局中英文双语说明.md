# HPClaw v0.1.13 全局中英文双语说明

生成时间：2026-08-15

## 功能

- 软件第一次启动默认显示中文。
- 登录页右上角和登录后的顶部工具栏都有语言按钮。
- 中文界面点击 `EN` 切换英文；英文界面点击 `中文` 切回中文。
- 语言选择保存在本机，重新打开软件后继续使用上次选择。
- 登录、顶部导航、AI、技能、流程、任务监控、作业监控、文件传输、预览、同步和弹窗提示使用同一个全局语言状态。
- 动态添加的弹窗、状态消息、占位文字、悬停提示和无障碍标签也会跟随切换。

## AI 与文献流程

- AI 请求携带当前界面语言。
- 英文模式下，Agent、快速问答、终端分析和新生成的文献流程会要求使用英文面向用户输出。
- 中文模式下继续使用中文回答和生成流程。
- 命令、集群路径、文件名、软件名、数据库名、原始终端输出和科学标识符保持原样，避免翻译破坏可执行性或科研含义。
- 已存在的历史对话和用户自定义内容保留原文；切换语言不会篡改历史记录。

## 验证

- 双语专项测试覆盖默认中文、切换英文、切回中文、语言持久化、动态文字与属性翻译，以及科研原始内容保护。
- Agent 提示测试覆盖默认中文回答和英文模式回答约束。
- 前端正式构建通过。
