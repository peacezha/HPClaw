# HPClaw v0.4.10 流程库清理与金标准固定脚本说明

发布日期：2026-09-19

## 流程库大清理：剔除重复、低质量流程

- 内置流程从 59 条精简到 **53 条**：BioSkills 种子里与 ENCODE 金标准重复的 6 条（atacseq-pipeline、chipseq-pipeline、rnaseq-to-de、hic-pipeline、clip-pipeline、smrna-pipeline）正式退役，同一分析只保留对应的 `encode-*` 版本。
- 退役流程的 SKILL.md 知识文档仍保留在 skills/ 目录中，AI 查资料（search_skills）不受影响；老用户库里的这 6 条会在本次升级后自动清除，用户自建流程不受影响。

## 每个流程第一步 = 真实可执行的环境检查

- 此前 ENCODE 流程的第一步是一段中文说明文字（`module av A B C 确认可加载；缺失先问用户`），不能直接运行。
- 现在 53 条内置流程的第一步统一是**可直接提交的环境检查脚本**（`server/workflows/envCheckScript.ts` 生成器，BioSkills 与 ENCODE 共用）：初始化 module 系统 → 逐个试加载清单软件（缺失时列出集群可用版本）→ 确认输入目录与可选参考数据 → 必需项缺失以非 0 退出并阻断后续步骤。
- 软件版本与流程 manifest 单一来源，版本精确钉死（如 Bowtie2/2.4.5、MACS2/2.2.7.1），避免两处漂移。

## 流程代码改为固定金脚本，AI 只微调参数

- 此前 BioSkills 流程的每一步是"执行合同"：要求 AI 现场读技能文档、重新生成代码——慢、费 token、质量不稳（用户实测"AI 容易多想做无用功"）。
- v3 导入器（`bioskillsSeed.ts`，BIOSKILLS_IMPORTER_VERSION=3）改为直接产出**固定可提交脚本**：`#BSUB` 作业头 + 按步骤内容匹配的 `module load` + 来源金标准命令体，可调参数（线程数、Q 值、阈值等）全部抽成 `{{参数}}`，AI 只确认/微调参数，不改写命令逻辑。
- 来源章节确实没有代码的少数步骤会明确标注"需要 AI 协助生成"，不再伪装成固定脚本。
- 修复 ATAC 等流程的实际断点：`qc/` 目录缺失补 `mkdir -p`（7 处）；中文尖括号占位符（`<重复1...>`、`<物种，如 hg38>` 等 12 处）全部替换为可运行的 glob 发现/参数化/条件跳过逻辑——此前这些步骤一跑就报 shell 重定向错误，是"ATAC 完全跑不了"的直接原因之一。

## 流程命令数上限彻底移除

- 定位到"12-command safety budget"中途暂停的根源：流程运行配置把命令预算压到 12 并写进系统提示词，模型读到后自行宣布"额度用完"暂停。服务端本来就已对正式流程豁免数值上限，这次把提示词里的误导数字一并移除，防死循环完全交给四类停滞判定（同命令重复/已成功变更/连续失败/连续无进展），与用户"不要限制命令数、自己判断死循环"的要求一致。

## 其他修补

- `module avail` 兜底、Ctrl+C/V 等保持不变；`builtin-disk-cleanup` 里的 `mv <确认可删的目录>` 占位改为明确的"等待用户确认"提示，避免误执行。

## 验证

- TypeScript 静态检查 0 错误。
- 78 个 ENCODE 步骤脚本逐条过 `bash -n` 语法检查，零中文占位符残留。
- 全量自动化测试通过（流程种子/分类/删除持久化/Agent 预算等断言已同步到 53 条新结构）。

## 植物适配：叶绿体去除与 SPOT 指标（ENCODE 动物默认项修正）

ENCODE 流程面向动物设计，只处理线粒体（chrM）；植物样本的叶绿体（chrC/plastid）reads 同样是高拷贝污染源，不去除会严重污染开放区域峰。本次修正：

- **encode-atacseq / encode-dnaseseq 新增全局参数 `ORGANELLE_REGEX`**：默认 `chrM|chrMT|chrC|chrPt|ChrM|ChrMt|ChrC|ChrPt|mitochondria|chloroplast|plastid`，覆盖常见细胞器染色体命名；清空该参数则保留细胞器 reads。过滤步骤同时输出 `qc/organelle.tsv`，分别统计线粒体、叶绿体、合计占比并进入 `qc.json`。
- **encode-atacseq 增加 SPOT 计算**：集群有 hotspot2 时自动以 BAM 头生成 chrom.sizes 并调用 hotspot2 得到 SPOT；没有 hotspot2 时明确 SKIP 并提示如何启用（FRiP 仍然计算）。
- ATAC/DNase 的 QC 关卡同步更新（细胞器占比 <20% 达标；SPOT≥0.3 达标），`docs/ENCODE生物信息分析与质控流程.md` 与代码全文文档已同步。
- 顺带修复：原有 `grep -c chrM` 在零匹配时会触发 `set -e` 误杀步骤的隐患（`|| true` 兜底）。
