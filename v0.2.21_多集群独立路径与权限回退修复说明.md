# HPClaw v0.2.21 修复说明

## 问题

文件传输的左、右栏只是端点显示槽位。旧实现切换端点后的第一个渲染周期仍带着上一个端点的路径，并把这条“正在加载”的路径提前写进新端点的路径记忆。例如从 `hpzhang` 切换到 `tmhan26` 时，可能使用 `tmhan26` 的 SSH 会话访问 `/public/home/hpzhang/...`，从而显示 `Permission denied`。

同时，远程目录列表虽然创建了 `AbortController`，但没有把 `AbortSignal` 传给 `fetch`，已经过期的旧集群请求仍可能在切换后覆盖新结果。

## 修复

- 每个端点（本地和每个 SSH session）独立保存最后一次成功打开的目录。
- `loading`、失败和切换中间态不再写入路径记忆。
- 首次进入集群端点时使用该 SSH 登录返回的真实 home 目录。
- 切换端点时取消上一个集群尚未完成的目录请求，避免旧响应覆盖新端点。
- 已保存目录后来失去权限时，自动清除该次失败并回退到当前账号自己的 home；如果 home 本身也无权限，才显示真实权限错误。
- 保留 v0.2.20 的结构化错误文本化修复，权限错误只显示提示，不会导致 React #31 白屏。

## 验证

- 覆盖 A → B → A 的独立路径恢复测试。
- 覆盖“切换加载态不得污染 B 路径”的竞态测试。
- 覆盖远程请求 AbortSignal 传递测试。
- 覆盖历史目录 Permission denied 后回退 B 自己 home 的组件测试。
