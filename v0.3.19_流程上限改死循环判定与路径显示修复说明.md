# HPClaw v0.3.19 流程上限改为死循环判定与路径显示修复说明

发布日期：2026-09-16

## 流程命令数上限移除，改为死循环自判定

- 按用户建议，正式流程**不再设数值型命令上限**（此前每轮 12 条，长流程总被中途掐断）。
- 替代机制是死循环检测：连续 2 轮没有执行任何新命令且 run.json 没有任何推进时，明确暂停并说明原因（不伪装成完成）；原有的同命令重复、已成功变更防重放、连续失败保护保持全局有效。模型步骤数上限（200）作为最后兜底保留。

## 图片/表格路径直接显示，不再是文本链接

- run.json 产物里的 **RUN 相对路径**（如 `results/plot.png`）此前无法定位，只显示一行文本、点击跳文件传输——现在统一解析为 RUN 绝对路径后直接出图/出表（缩略图点击全屏、CSV 一键表格预览）。
- 对话正文中的**裸相对路径**（`results/x.png`、`code/step-01.sh`）也会被识别并按流程 RUN 目录解析，能显示图片/表格卡片就不再只给文字。

## 建议选项按上下文生成

- 不再固定给"验证结果是否完整/查看关键输出文件/整理成可复用流程"三条。现在根据对话当前状态给方向：
  - 环境缺失 → 一键部署缺失环境 / 查看缺失清单 / 装好后继续
  - 暂停或死循环保护 → 继续执行流程 / 查看当前步骤 / 换个思路
  - 作业排队中 → 查看作业状态 / 作业完成后继续
  - 完成 → 验证结果 / 查看输出 / 整理成可复用流程
  - 失败 → 定位根因 / 安全修复 / 需补充信息

## 验证

- TypeScript 静态检查通过（0 错误）。
- 全量自动化测试通过（死循环停滞判定、无命令上限语义、相对路径解析、裸相对路径提取等用例已同步）。
