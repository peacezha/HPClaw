# HPClaw v0.1.6 AI 生命周期与集群稳定性修复

## 本次实际故障证据

- 故障时日志停在 Agent 流已启动、等待模型输出，没有 SSH 客户端错误或主连接关闭记录。
- 使用当前 `deepseek-v4-pro` 和 API Key 分别验证了模型列表、原始流式请求和 HPClaw Agent 工具链；供应商、模型和 Key 均正常。
- 真正的缺口在生命周期：服务端只发送 abort，但某些供应商的异步流不保证结束 `iterator.next()`；前端收到 error 事件后又仍等待连接 EOF，因此界面可持续显示“生成中”。
- 应用关闭后又会连带结束内存中的 SSH 会话，用户观感就是“AI 卡住，然后集群也断开”。

## 修复后的 AI 行为

1. Agent 流由 HPClaw 自己控制收尾，不再依赖供应商是否响应 AbortSignal。
2. 45 秒内没有任何有效模型响应时，本轮 Agent 自动结束并给出可重试提示。
3. 已开始输出后连续 135 秒无新消息时，判定模型流停滞并收尾。
4. 5 分钟总看门狗会直接关闭 SSE，即使模型库卡死也不会无限生成。
5. 前端收到 error 立即清除加载和流式状态，用户可立即重试或改用快速模式。
6. 界面实时显示“整理上下文”、“请求模型”、“推理规划”、“执行集群命令”等阶段，不再只显示含糊的“生成中”。

## 集群连接稳定性

- AI 超时和 AI 错误只结束当前 AI HTTP 流，不调用 SSH `close()`。
- SSH 真正断开时，日志会区分 `client-error`、`remote-transport-close` 和 `local-close`，便于下次直接定位。
- Electron 只允许一个桌面实例，避免旧版和新版多窗口共享用户数据并干扰会话。
- 后台监视 Electron 父进程；桌面主进程崩溃后，后台会自动释放端口，不再留下“假在线”孤儿进程。
- 启动日志不再每次覆盖，而是按启动分段保留并在 5 MB 时轮换。

## 验证结果

- TypeScript 类型检查通过。
- 81 个测试文件、556 项测试全部通过。
- 真实 DeepSeek 原始流式请求约 1.8 秒返回正文。
- 真实 HPClaw Agent 工具链约 1.7 秒完成并正确发送 done。
- v0.1.6 免安装软件真实启动，内置 HTTP 服务返回 200。
- 模拟桌面主进程异常结束后，后台进程与监听端口均已自动清理。
