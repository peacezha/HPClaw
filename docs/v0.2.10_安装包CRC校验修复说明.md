# HPClaw v0.2.10 安装包 CRC 校验修复

- 停用 CRC 损坏的 v0.2.9 安装包。
- 安装包构建前强制移除同版本中间压缩文件，禁止复用压缩崩溃留下的半成品。
- Windows 安装包固定使用低内存、单线程压缩，规避本机 7-Zip `0xc0000005` 及并行压缩静默 CRC 损坏。
- 构建完成后自动执行完整 CRC 测试；`HPClaw.exe` 或 `resources/app.asar` 任一损坏都会阻止交付。
- 保留 v0.2.9 的两个 HiDOG V11 流程优化。
