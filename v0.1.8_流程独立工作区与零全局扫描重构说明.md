# HPClaw v0.1.8：流程独立工作区与零全局扫描

## 现场问题

真实流程运行明明已经创建了 `hpclaw_flows/<流程>/.../<runId>`，Agent 仍然离开运行目录，对用户主目录执行 `find`、`du`、多次 `ls`、文件计数、抽样校验和无效作业查询。这些动作既不是流程步骤，也不是用户要求，扩大了上下文和 SSH 压力，并增加 AI 提前断流的概率。

## 新执行边界

- 每次流程运行绑定唯一 RUN 目录；脚本、日志、状态和结果全部留在 RUN 内。
- 服务端给每条正式流程命令自动加上 RUN 工作目录，不依赖模型自觉 `cd`。
- 正式流程禁止自行切换目录、访问父目录，以及对未授权路径使用 `find`、`du`、`ls`、`tree`、`locate`。
- 用户在运行面板选择的输入和参考路径作为只读白名单；流程不得为了“确认数据”扫描主目录。
- `workspace.json` 把可写根目录和只读输入边界随运行一起保存，便于审计。

## 去除无用前置工作

- 正式流程不再构建通用技能包、不注入历史会话摘要、不刷新集群快照。
- 流程步骤就是唯一计划，不再额外调用 `set_plan` 生成一套重复计划。
- Agent 每次只用 `get_workflow_step` 读取当前步骤，不一次加载全部步骤。
- 已有明确步骤命令时不做通用技能搜索；技能搜索只读内存缓存，不触发远程文件扫描。
- 默认集群快照不再执行 `ls` 或 `find`；普通问答只保留位置、作业和配额等轻量状态。
- 不再默认生成文件总数、总大小、抽样 md5、`inputs.md`、归档副本或报告；只有流程步骤或用户明确要求时才执行。

## 运行监控

- 新运行写入 `~/hpclaw_flows/.hpclaw/run-index.txt` 小型索引。
- 页面刷新和作业完成对账只读取索引中明确登记的 `run.json`，不再 glob 遍历所有流程目录。
- 历史运行目录发现改为用户手动点击“导入旧记录”后执行，正常运行不会自动扫描。
- 步骤状态仍通过 Socket 实时推送；60 秒快照只读取已登记运行，作为断线补偿。

## 页面精简

- 运行抽屉首先显示“专属工作目录 / 强制隔离”。
- 数据选择、必要参数和运行按钮保留在主视图。
- 环境检查改为按需、默认折叠；步骤证据、可选项和命令覆盖归入“步骤与高级设置”。
- 每条运行记录展开后明确显示自己的工作目录。

## 验证

- 新增正式流程目录绑定、扫描拦截、运行索引、执行标记和精简流程消息测试。
- 全项目 84 个测试文件、569 项测试全部通过。
- TypeScript 类型检查通过。
