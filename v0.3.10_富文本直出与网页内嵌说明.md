# HPClaw v0.3.10 富文本直出与网页内嵌说明

发布日期：2026-09-16

## 图片路径直接出图

- "检测到的文件"横条里的图片路径不再只是文本：直接内联显示图片（本地/集群自动路由、失败跨端点重试），点击可全屏放大，路径与下载按钮保留；助手消息不重复出图（图片卡已覆盖），系统通知里的图片也能显示了。
- 执行过程折叠卡里的工具结果，展开后对其中的图片/表格路径自动出卡片（折叠时不渲染，不拖累性能）。
- 流程运行卡的输出产物：图片显示缩略图（点击放大），CSV/TSV 可一键表格预览。
- 图片自动显示阈值从 500KB 提高到 3MB，绝大多数结果图不再要点"加载图片"。
- 修复本地会话下横条下载按钮发错通道（必然 404）的问题。

## 网页内嵌交互

- 答复中的 http(s) 链接自动生成"网页预览卡"（域名 + URL，最多 2 个），默认收起不加载外网；点击展开后页面**内嵌在对话里，可交互**（Electron 用 webview，与侧边栏同分区；浏览器开发模式回退沙箱 iframe）。
- 每张卡都有"在侧边打开"和"在浏览器打开"入口，三种用法随意切换。
- 侧边网页栏打开集群/本地生成的 HTML 报告时默认启用脚本（自己的结果网页，交互图表需要 JS），保留手动开关；CSP 仍禁外部网络。

## 验证

- TypeScript 静态检查通过（0 错误）。
- 全量自动化测试通过：143 个测试文件，共 1024 项测试（新增横条出图、执行过程富内容、网页预览卡、输出区缩略图等 24 项）。
- 真实页面走查：示例对话中表格/图片/网页预览卡全部正确渲染，内嵌页面可展开加载。
